server {
    listen 443;
    server_name {{ domain }};
    
    location / {
        return 301 http://$host$request_uri;
    }
}

server {
    listen 80;
    server_name {{ domain }};
    
    location / {
        root /var/www/{{ domain }}/html;
        index index.html;
    }
}
